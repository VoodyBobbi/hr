# Всё, что нужно проекту. Один файл, отдельного для разработки нет
# намеренно: pytest весит около 5 МБ рядом с torch на 300 МБ и моделью
# поиска на 470 МБ, а лишний файл — это лишний вопрос «а этот зачем».
#
# Ставится автоматически при `python start.py`, вручную запускать не нужно.

fastapi==0.115.0
uvicorn[standard]==0.30.6
gigachat==0.2.1
httpx==0.28.1
faiss-cpu==1.14.3
sentence-transformers==5.6.0
python-telegram-bot==22.8
python-dotenv==1.0.1
cryptography==46.0.6
slowapi==0.1.10

# Нужен только для запуска тестов командой `python -m pytest`.
# На работу бота не влияет.
pytest==8.3.4
